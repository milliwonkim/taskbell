# 프로젝트 지침

- 항상 한국어로 답변한다.
- 앱 코드를 변경할 때는 빌드 시점 오류와 발생 가능성이 높은 런타임 오류를 모두 검증한다.
- iOS/SwiftUI/SwiftData 작업에서는 `xcodebuild` 성공만으로 멈추지 않는다. `ModelContainer` 생성 실패, SwiftData 스키마/저장소 마이그레이션 문제, CloudKit 권한 또는 컨테이너 불일치, 알림 권한 실패, 시뮬레이터/기기의 오래된 앱 데이터 같은 런타임 위험도 함께 확인한다.
- 모든 작업이 완료되면 Xcode의 플레이 버튼을 누르는 것과 같은 동작을 수행해 앱을 실행한다.
- 런타임 크래시가 보고되면 먼저 정확한 크래시 메시지를 기준으로 재현하거나 원인을 추론한 뒤, 가장 작은 수정만 적용하고 다시 빌드한다.
